# Copy this file to src/resources/app.properties and fill in values for your environment
# Example properties for the electronics shop application

db.url=jdbc:mysql://localhost:3306/electronic_shop
db.username=root
db.password=
db.driver=com.mysql.cj.jdbc.Driver
# Optional: whether to run DB migrations automatically at startup (true/false). When using Flyway via Gradle, migrations run automatically with the fat JAR.
# db.init=true
